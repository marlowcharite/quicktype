{
    "type": "object",
    "properties": {
        "lvc": {
            "enum": ["lawful", "neutral", "chaotic"]
        },
        "gve": {
            "type": "string",
            "enum": ["good", "neutral", "evil"]
        }
    },
    "required": ["gve"]
}
